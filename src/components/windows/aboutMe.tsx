import AppWindow from '../appWindow';

const AboutMe = () => {
    return (
        <AppWindow window='about_me'>
            <div>This is a window all about Nicholas Evans</div>
        </AppWindow>
    );
};

export default AboutMe;
